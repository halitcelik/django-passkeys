import element from "https://stephen.band/dom/modules/element.js";

export element(
    "passkey-login", {
    construct: function (shadow, internals) {
        console.log("CONSTRUCT", this, shadow, internals)
    },

}, {});

